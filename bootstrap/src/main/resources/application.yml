spring:
  application:
    name: material-management-service
  jackson:
    deserialization:
      fail-on-unknown-properties: false
    serialization:
      write-dates-as-timestamps: false

  data:
    mongodb:
      uri: mongodb+srv://material-management:AjayaApurba1@cluster0.p2qoo.mongodb.net/test

#      // streams and rabbit mq config(routing key)
